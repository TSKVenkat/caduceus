---
type: Convention
title: pipe-wrap convention
description: How to transform a string under the pipe-wrap convention.
---

# pipe-wrap convention

To transform a string under the **pipe-wrap** convention: lowercase the input and wrap it between pipe characters, e.g. `|...|`.

Examples:

- "Alpha" -> "|alpha|"
- "Two Words" -> "|two words|"
