---
type: Convention
title: slug convention
description: How to transform a string under the slug convention.
---

# slug convention

To transform a string under the **slug** convention: lowercase the input, replace spaces with hyphens, and prefix with `s/`.

Examples:

- "Alpha" -> "s/alpha"
- "Two Words" -> "s/two-words"
