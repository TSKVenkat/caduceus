Implement transform(input) in transform.js so it follows the project's "reverse-tag" string convention. The exact rule is described in the project knowledge — consult it, then implement transform accordingly.
